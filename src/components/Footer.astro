---
import { Icon } from 'astro-icon/components';
import { business } from '../data/business';
import { serviceTypes } from '../data/serviceTypes';
import { serviceAreas } from '../data/serviceAreas';
import { serviceUrl, areaUrl } from '../lib/urls';
---

<footer class="bg-brand-950 text-white">
  <!-- Subtle top border gradient -->
  <div class="h-px w-full bg-gradient-to-r from-transparent via-brand-500/40 to-transparent" />

  <div class="mx-auto max-w-7xl px-5 pb-10 pt-16 lg:px-8">
    <!-- Top section: brand + tagline -->
    <div class="border-b border-white/10 pb-10">
      <a href="/" class="group inline-flex items-center gap-2.5">
        <img
          src="/images/hero-homepage.webp"
          alt="K2 Curtains and Trading"
          class="h-10 w-auto rounded"
          width="1376"
          height="768"
        />
        <span class="font-display text-xl font-bold text-white">K2 Curtains</span>
      </a>
      <p class="mt-4 max-w-md text-sm leading-relaxed text-stone-400">
        {business.tagline} Custom curtains, blinds, flooring, and upholstery for homes and villas across Abu Dhabi.
      </p>
    </div>

    <!-- Link columns -->
    <div class="grid gap-8 pt-10 sm:grid-cols-2 lg:grid-cols-3">
      <!-- Column 1: Our Services -->
      <div>
        <h3 class="mb-4 text-xs font-semibold uppercase tracking-wider text-stone-500">
          Our Services
        </h3>
        <ul class="space-y-2.5">
          {serviceTypes.map((service) => (
            <li>
              <a
                href={serviceUrl(service.slug)}
                class="text-sm text-stone-400 transition-colors hover:text-white"
              >
                {service.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Column 2: Service Areas -->
      <div>
        <h3 class="mb-4 text-xs font-semibold uppercase tracking-wider text-stone-500">
          Service Areas
        </h3>
        <ul class="space-y-2.5">
          {serviceAreas.map((area) => (
            <li>
              <a
                href={areaUrl(area.slug)}
                class="text-sm text-stone-400 transition-colors hover:text-white"
              >
                {area.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Column 3: Contact Info -->
      <div>
        <h3 class="mb-4 text-xs font-semibold uppercase tracking-wider text-stone-500">
          Contact Info
        </h3>
        <ul class="space-y-3">
          <li class="flex items-start gap-2.5">
            <Icon name="lucide:map-pin" class="mt-0.5 size-4 shrink-0 text-stone-500" />
            <span class="text-sm text-stone-400">
              {business.address.street}, {business.address.city}, {business.address.state}
            </span>
          </li>
          <li class="flex items-start gap-2.5">
            <Icon name="lucide:phone" class="mt-0.5 size-4 shrink-0 text-stone-500" />
            <a
              href={business.phoneHref}
              class="text-sm text-stone-400 transition-colors hover:text-white"
            >
              {business.phone}
            </a>
          </li>
          {business.phoneSecondary && (
            <li class="flex items-start gap-2.5">
              <Icon name="lucide:phone" class="mt-0.5 size-4 shrink-0 text-stone-500" />
              <a
                href={business.phoneSecondaryHref}
                class="text-sm text-stone-400 transition-colors hover:text-white"
              >
                {business.phoneSecondary}
              </a>
            </li>
          )}
          <li class="flex items-start gap-2.5">
            <Icon name="lucide:message-circle" class="mt-0.5 size-4 shrink-0 text-stone-500" />
            <a
              href={business.whatsappHref}
              target="_blank"
              rel="noopener noreferrer"
              class="text-sm text-stone-400 transition-colors hover:text-white"
            >
              WhatsApp {business.whatsapp}
            </a>
          </li>
          <li class="flex items-start gap-2.5">
            <Icon name="lucide:mail" class="mt-0.5 size-4 shrink-0 text-stone-500" />
            <a
              href={`mailto:${business.email}`}
              class="text-sm text-stone-400 transition-colors hover:text-white"
            >
              {business.email}
            </a>
          </li>
          <li class="flex items-start gap-2.5">
            <Icon name="lucide:clock" class="mt-0.5 size-4 shrink-0 text-stone-500" />
            <div class="text-sm text-stone-400">
              {business.hours.map((schedule) => (
                <p>{schedule.days}: {schedule.hours}</p>
              ))}
            </div>
          </li>
        </ul>
      </div>
    </div>

    <!-- Map embed -->
    <div class="mt-10">
      <h3 class="mb-3 text-xs font-semibold uppercase tracking-wider text-stone-500">
        Find Us
      </h3>
      <div class="overflow-hidden rounded-xl border border-white/10">
        <iframe
          title="K2 Curtains location on Google Maps"
          src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3631.5584!2d54.69010095713209!3d24.401756664626475!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!5e0!3m2!1sen!2sae!4v1694784000000!5m2!1sen!2sae"
          width="100%"
          height="250"
          style="border:0;"
          allowfullscreen=""
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
        ></iframe>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="mt-10 flex flex-col items-center justify-between gap-3 border-t border-white/10 pt-6 sm:flex-row">
      <p class="text-xs text-stone-600">
        &copy; {new Date().getFullYear()} {business.legalName}. All rights reserved.
      </p>
      <p class="text-xs text-stone-600">
        Custom curtains, blinds & home décor. Abu Dhabi. <a href="https://www.curtainshopabudhabi.com" class="text-stone-500 hover:text-white transition-colors">curtainshopabudhabi.com</a>
      </p>
    </div>
  </div>
</footer>
